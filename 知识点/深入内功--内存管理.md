## Linux 内核如何检测可用物理内存地址范围
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/82b83fceaa47ff1530847e18719a0b2f_MD5.jpeg)
固件（firmware）是位于主板上的使用 SPI Nor Flash 存储着的软件。起着在硬件和操作系统中间承上启下的作用。它对外提供接口规范是高级配置和电源接口（ ACPI，Advanced Configuration and Power Interface）。
计算机启动过程中，固件负责硬件自检、初始化硬件设备、加载 OS 引导程序，将控制权转移到 OS 并提供接口供 OS 读取硬件信息。

### 从固件读取内存布局数据
内核请求中断号 15H，并设置操作码为 E820 H。然后固件就会向内核报告可用的物理内存地址范围。因为操作码是 E820，所以这个获取机制也被常称为 E820。

内核启动有 main 函数，其中会调用 detect_memory函数。
```cpp
//file:arch/x86/boot/main.c  
void main(void)  
{  
 detect_memory();  
 ...  
}  
  
// file:arch/x86/boot/memory.c  
void detect_memory(void)  
{  
 detect_memory_e820();  
 ...  
}
```
detect_memory_e820 函数发出 15 中断并处理所有结果，把内存地址范围保存到 boot_params.e820_table 对象中。
```cpp
//file:arch/x86/boot/memory.c  
static void detect_memory_e820(void)  
{  
 struct boot_e820_entry *desc = boot_params.e820_table;  
  
 initregs(&ireg);  
 ireg.ax  = 0xe820;  
 ...  
  
 do {  
  intcall(0x15, &ireg, &oreg);  
  ...  
  *desc++ = buf;  
  count++;  
 }while (ireg.ebx && count < ARRAY_SIZE(boot_params.e820_table));  
  
 boot_params.e820_entries = count;  
}
```
内存最终会存储在 e820_table这个全局数据结构中。

### 总结
Linux 操作系统在启动的时候会向固件程序 发起 15 号中断，并设置操作码为 E820 H。然后固件上报内存地址范围，内核会把它保存到 e820_table 并打印出来。
有了这个 e820_table 表示的可用物理内存地址范围后，后面内核会通过自己的初期分配器 memblock、以及伙伴系统两种机制将所有可用的物理内存管理起来。
其中 memblock 管理方式比较粗放，仅仅用于内核启动时。启动好了后 memblock 会把可用物理页交接给伙伴系统。伙伴系统管理的比较精细，会按 4KB、8KB、16KB 等各种不同大小的空闲块来管理。
当我们的应用程序执行过程中，发现对应虚拟地址对应物理内存还没有分配的时候，会触发缺页中断。在缺页中断中向伙伴系统申请指定大小的物理内存页面。

## 内核怎么使用内存
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/05722f1f4c1d65347e36952259f2d4f9_MD5.jpeg)

### NODE 划分
现代服务器内存和 CPU 都是 NUMA 架构。
每一个 CPU 以及和它直连的内存条组成一个**node（节点）**。
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/56fee9a97f43da57b3c6395042742a19_MD5.jpeg)

### ZONE 划分
每个 node 又会划分成若干的 **zone（区域）** 。zone 表示内存中的一块范围
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/36f4e9379a950a9d8fce7b1c8db08158_MD5.jpeg)
- ZONE_DMA：地址段最低的一块内存区域，ISA(Industry Standard Architecture)设备DMA访问
- ZONE_DMA32：该Zone用于支持32-bits地址总线的DMA设备，只在64-bits系统里才有效
- ZONE_NORMAL：在X86-64架构下，DMA和DMA32之外的内存全部在NORMAL的Zone里管理
在每个zone下，都包含了许许多多个 Page（页面）， 在linux下一个Page的大小一般是 4 KB。
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/f08c2004700bee21fcb41ef9aee067f4_MD5.jpeg)

### 基于伙伴系统管理空闲页面
每个 zone 下面都有如此之多的页面，Linux使用**伙伴系统**对这些页面进行高效的管理。在内核中，表示 zone 的数据结构是 `struct zone`。其下面的一个数组 free_area 管理了绝大部分可用的空闲页面。这个数组就是**伙伴系统**实现的重要数据结构。
free_area是一个11个元素的数组，在每一个数组分别代表的是空闲可分配连续4K、8K、16K、......、4M内存链表。
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/3c8ff65c4d06fa53c8a4d4e3084d6c88_MD5.jpeg)
内核提供分配器函数 `alloc_pages` 到上面的多个链表中寻找可用连续页面。
alloc_pages 的工作步骤：
例如要申请 8k 的内存
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/185e8e9953ac05c3b320cc9e172e301c_MD5.jpeg)
基于伙伴系统的内存分配中，有可能需要将大块内存拆分成两个小伙伴。在释放中，可能会将两个小伙伴合并再次组成更大块的连续内存。

### SLAB 管理器
目前介绍的内存分配都是以页面（4KB）为单位的。对于内核中使用的对象来说，多大的对象都有，如果全以 4KB 分配，太过于浪费。
在伙伴系统之上，**内核又给自己搞了一个专用的内存分配器， 叫slab或slub**。
这个分配器最大的特点就是，一个slab内只分配特定大小、甚至是特定的对象。这样当一个对象释放内存后，另一个同类对象可以直接使用这块内存。通过这种办法极大地降低了碎片发生的几率。
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/4f0b078bfec34d108d9a0dab4542bbfe_MD5.jpeg)
slab 的内核对象定义为：
```cpp
//file: include/linux/slab_def.h  
struct kmem_cache {  
    struct kmem_cache_node **node  
    ......  
}  
  
//file: mm/slab.h  
struct kmem_cache_node {  
    struct list_head slabs_partial;   
    struct list_head slabs_full;  
    struct list_head slabs_free;  
    ......  
}
```
每个cache都有满、半满、空三个链表。每个链表节点都对应一个 slab，一个 slab 由 1 个或者多个内存页组成。
**在每一个 slab 内都保存的是同等大小的对象。** 一个cache的组成示意图如下：
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/79c3b8d8d2c3e7e017df158f3dbf7239_MD5.jpeg)
当 cache 中内存不够的时候，会调用基于伙伴系统的分配器（__alloc_pages函数）请求整页连续内存的分配。

内核中有多个 kmem_cache 存在，在 Linux 初始化或运行过程中分配，有的是专用的，有的是通用的。
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/3b1b0712e803e8ffbea9952191e7e3b9_MD5.jpeg)
### 总结
![](%E7%9F%A5%E8%AF%86%E7%82%B9/attachments/db09a9c752140f15a0ad3c5934d69c5c_MD5.jpeg)