类似于今日头条的咨询类项目，采用前后端分离模式开发，整个项目整体分为平台管理平台、用户自媒体平台以及移动 APP 端。
前端整体使用 Vue2 开发，app 采用阿里的 weex 框架，可以提供 h5、android 以及 ios 的发布方案。
后端主要使用SpringBoot+SpringCloud 的微服务架构，nginx 接收用户请求，反向代理到网关 SpringCloud Gateway，注册中心和配置中心使用的是 nacos，服务之间调用使用 feign rpc，整合了客户端负载均衡 ribbon，使用 hystrix 做服务熔断，seata 做分布式事务，xxljob 处理分布式调度；核心数据存 mysql，redis 做缓存，es 做检索，使用 mongodb 存储非结构化数据，kafka 做日志收集和流式计算。

## 功能架构图
![](attachments/3fdc0e64bd4405ee5d180984b20d0c07_MD5.jpeg)

## 技术结构图
![](attachments/3347539c08edcf11b69eb9fe13073165_MD5.jpeg)
## 热点计算
前端埋点，日志采集用户点击、收藏等数据，flume 进行日志采集进入大数据分析链路，最终将结果保存到 clickhouse 中，定时任务 2min 查询 clickhouse 回写 redis。
> 多级缓存实现：[百万级QPS多级缓存架构实战：从设计到落地的完整指南-CSDN博客](https://blog.csdn.net/weixin_42079748/article/details/153969986)

采用 Nginx+Caffnine+Redis 实现多级缓存，Redission 防止并发回源。
## ES实现冷热分离
Flink CDC+Kafka同步增量数据，ES 实现冷热分离（把索引按访问频率拆到不同硬件配置的节点上，热节点用 SSD 承接最新高频数据，冷节点用 HDD/廉价云盘存归档数据，从而兼顾查询性能与存储成本。）。
实现：
1. 节点打标签：hot、warm、cold
2. 索引模板：新索引默认落在 hot 节点
3. ILM 策略：数据 0–3 天 hot → 3–30 天 warm → 30–90 天 cold → 90 天 delete
4. 查询侧：近期走 hot* 别名，历史走 cold* 别名，或让客户端同时查别名由 ES 内部路由

## 为什么使用Flink CDC 而不是 Canal？
- 端到端 Exactly-Once
借助 Flink Checkpoint 机制，Flink CDC 能做到端到端 Exactly-Once 语义；Canal 自身只保证 At-Least-Once，下游如果要做去重或幂等，需要额外开发逻辑
- 实时性与吞吐量
流式算子配合并行度调节，吞吐高；Canal 采用单线程或有限线程池解析，高并发场景容易遇到瓶颈
- 资源占用权衡
虽然 Flink CDC 对 CPU、内存需求更高，但节省了 Canal-Server+Kafka 这一层的机器与运维成本；整体 TCO 在大规模场景下反而更低

## 联想词的实现
目前项目是通过 mongodb + 正则搜索 实现的，联想词是静态的，无法自主动态添加。
后续实现：
联想词主要分为：前缀匹配、模糊纠错、语义联想
通过 ES 实现，ES 索引prefix、fuzzy 以及 semantic。
查询时三层降级，prefix 能召回，直接返回；前缀无结果，fuzzy 兜底纠错，最后是semantic 召回。
超热的词可以加载到==内存 Trie+CDN==，屏蔽 ES 流量。